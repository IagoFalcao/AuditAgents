{
    "report_fp": "112.md",
    "analysis_resuls": [
        {
            "vulnerability_type": "Race Condition",
            "affected_functions": [
                "transfer()"
            ],
            "root_cause": "The user checkpoints occur after the balances are updated in the `transfer()` function, allowing a user to send funds between their own accounts and repeatedly claim maximum rewards.",
            "impact": "A user can steal all rewards due to checkpoint after transfer",
            "recommendation": "Ensure that user checkpoints occur before balance updates. This can be achieved by moving the call to `ILpGauge(lpGauge).userCheckpoint()` earlier in the function or by using a different approach to update balances and checkpoints.",
            "affected_contracts": [
                "StakerVault.sol"
            ],
            "attack_vector": "User can send funds between their own accounts and repeatedly claim maximum rewards due to checkpoint after transfer.",
            "prerequisites": "The user must have the ability to call `ILpGauge(lpGauge).userCheckpoint()`",
            "cwe": "CWE-193",
            "swc": "SWC-04"
        },
        {
            "vulnerability_type": "Improper Function Call",
            "affected_functions": [
                "TopUpActionLibrary.lockFunds"
            ],
            "root_cause": "The function `lockFunds` in the `TopUpActionLibrary` does not call `stakerVault.increaseActionLockedBalance` when transferring stakes from a payer, leading to an incorrect update of the action locked balance for that payer.",
            "impact": "This vulnerability can cause serious fund loss and theft due to incorrect calculations based on the updated action locked balance. It also allows bypasses of certain restrictions.",
            "recommendation": "Implement `stakerVault.increaseActionLockedBalance` when transferring stakes from a payer in the `lockFunds` function to ensure accurate updates and prevent potential fund loss and restriction bypasses.",
            "cwe": "CWE-264: Improper Error Handling",
            "swc": "SWC-10: Improper Function Call"
        }
    ]
}