{
    "report_fp": "97.md",
    "analysis_resuls": [
        {
            "vulnerability_type": "Reentrancy",
            "affected_functions": [
                "depositErc20"
            ],
            "root_cause": "The `depositErc20` function allows setting `tokenAddress = NATIVE` and does not throw an error. The `SafeERC20Upgradeable.safeTransferFrom(IERC20Upgradeable(tokenAddress), sender, address(this), amount);` call is performed as a low-level call to the EOA (External Owned Account) at 0xEeeeeEeeeEeEeeEeEeEeeEEEeeeeEeeeeeeeEEeE. This call always succeeds due to the `safe*` version being used.",
            "impact": "An attacker can deposit infinite native tokens without paying anything, leading to a loss of funds on the other chain.",
            "recommendation": "Implement proper reentrancy protection mechanisms such as using `require` statements or adding checks before calling external functions. Ensure that all calls are properly validated and revert if necessary.",
            "cwe": "CWE-56",
            "swc": "SWC-12"
        },
        {
            "vulnerability_type": "Design",
            "affected_functions": [
                "removeLiquidity"
            ],
            "root_cause": "The share price of the LP can be manipulated to an extremely low value, leading to potential overflow issues when converting shares into token amount during liquidity removal.",
            "impact": "Users' funds may be frozen due to potential overflow issues when removing liquidity.",
            "recommendation": "Implement proper checks on the share price before converting it into token amount. Ensure that the conversion does not result in an overflow condition.",
            "affected_contracts": [
                "LiquidityProviders.sol"
            ],
            "attack_vector": "Manipulating the share price to a low value, leading to potential overflow issues during liquidity removal.",
            "prerequisites": "The share price is manipulated to an extremely low value, making it possible for `sharesToTokenAmount(totalNFTShares, _tokenAddress)` to overflow in `removeLiquidity()` and therefore freeze users' funds.",
            "cwe": "CWE-190: Improper Input Validation",
            "swc": "SWC-23: Lack of Proper Checks"
        }
    ]
}