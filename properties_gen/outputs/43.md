{
    "report_fp": "43.md",
    "analysis_resuls": [
        {
            "vulnerability_type": "Improper Use of Library",
            "affected_functions": [
                "onlyOwner"
            ],
            "root_cause": "Using an incorrect version of the `Ownable` library (non-upgradeable) instead of the upgradeable version.",
            "impact": "All `onlyOwner` functions will be inaccessible due to the lack of owner when deployed as a proxy contract.",
            "recommendation": "Replace the non-upgradeable `Ownable` library with the upgradeable version (`@openzeppelin/contracts-upgradeable/access/OwnableUpgradeable.sol`) to ensure proper functionality and maintainability.",
            "affected_contracts": [
                "DelegatedStaking.sol"
            ],
            "attack_vector": "Proxy-based upgradeability system requires no constructors, leading to the absence of owner when deployed as a proxy contract.",
            "prerequisites": "The current implementation uses an non-upgradeable version of `Ownable` library instead of the upgradeable version.",
            "cwe": "CWE-259: Improper Use of Library",
            "swc": "SWC-10: Improper Use of External Libraries"
        },
        {
            "vulnerability_type": "Race Condition",
            "affected_functions": [
                "unstake"
            ],
            "root_cause": "The `unstake` function does not immediately update the exchange rates. It first computes the `validatorSharesRemove = tokensToShares(amount, v.exchangeRate)` with the old exchange rate before updating the exchange rates.",
            "impact": "This vulnerability allows an attacker to exploit a race condition by manipulating the exchange rates in such a way that they are updated after the validator has already unstaked their shares. This can lead to unexpected behavior or even loss of funds for validators.",
            "recommendation": "The `unstake` function should update the exchange rates immediately after computing the `validatorSharesRemove`. This ensures that the exchange rates reflect the current state and prevents any potential race conditions.",
            "cwe": "CWE-193",
            "swc": "SWC-02"
        }
    ]
}