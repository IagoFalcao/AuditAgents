{
    "report_fp": "191.md",
    "analysis_resuls": [
        {
            "vulnerability_type": "Race Condition",
            "affected_functions": [
                "lastResortTimelockOwnerClaimNFT"
            ],
            "root_cause": "The `recoverTimelock` is set in the `initialize(...)` function and not updated by any other means, allowing the admin to call `lastResortTimelockOwnerClaimNFT()` immediately after initialization.",
            "impact": "This vulnerability allows an attacker to prematurely withdraw a NFT that has been raffled but not claimed by users within the specified timelock period. The attack can be executed without any prerequisites, as long as the admin calls `lastResortTimelockOwnerClaimNFT()` after initialization.",
            "recommendation": "Implement proper synchronization between `recoverTimelock` and `startDraw()`, ensuring that the admin cannot call `lastResortTimelockOwnerClaimNFT()` until the draw is initiated. This can be achieved by updating `recoverTimelock` in the `startDraw()` function.",
            "cwe": "CWE-400",
            "swc": "SWC-12"
        },
        {
            "vulnerability_type": "Vulnerable to Front-Running",
            "affected_functions": [
                "startDraw",
                "redraw"
            ],
            "root_cause": "Host can fund a subscription with insufficient LINK balance, allowing them to invalidate the assumption that the Chainlink response has been received within the 1-hour redraw cooldown period.",
            "impact": "This vulnerability allows an attacker to manipulate the draw outcome by frontrunning the Chainlink request and invalidating the randomness provided in the original request.",
            "recommendation": "Ensure that the host maintains a sufficient LINK balance for their subscription. This prevents the host from manipulating the randomness after making a request.",
            "affected_contracts": [
                "RandomDraw"
            ],
            "attack_vector": "Host can fund a subscription with insufficient LINK balance, allowing them to invalidate the assumption that the Chainlink response has been received within the 1-hour redraw cooldown period.",
            "prerequisites": "The host must have a subscription ID and sufficient LINK balance for their request. The vulnerability is triggered when the subscription balance falls below the minimum required balance.",
            "cwe": "CWE-467: Host can manipulate randomness by frontrunning requests",
            "swc": "SWC-01: Ensure subscriptions have sufficient funds to process requests"
        }
    ]
}