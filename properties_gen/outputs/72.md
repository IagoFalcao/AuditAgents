{
    "report_fp": "72.md",
    "analysis_resuls": [
        {
            "vulnerability_type": "Hardcoded Gas Limit",
            "affected_functions": [
                "OpenLevV1Lib.doTransferOut",
                "LPool.doTransferOut"
            ],
            "root_cause": "The `payable.transfer()` function is used to handle the transfer of wrapped native tokens, which can fail due to hard-coded gas limits.",
            "impact": "Any programmatical usage of OpenLevV1 and LPool is at risk. This includes user's principal funds freeze scenario, leading to a high severity issue.",
            "recommendation": "Replace `payable.transfer()` with an alternative method that does not have hard-coded gas limits or modify the smart contract to handle fallback functions correctly.",
            "cwe": "CWE-264: Hardcoded Limit on Function Call",
            "swc": "SWC-10: Gas Limit Violation"
        },
        {
            "vulnerability_type": "Improper Input Validation",
            "affected_functions": [
                "uniClassSell"
            ],
            "root_cause": "The contract trusted the result given by `getAmountOut()` instead of verifying it against expected values.",
            "impact": "This can lead to an output amount being fewer than `minBuyAmount`, resulting in a loss for the user.",
            "recommendation": "Implement proper input validation and ensure that the received amount is verified against expected values before proceeding with any transactions.",
            "affected_contracts": [
                "UniV2ClassDex.sol"
            ],
            "attack_vector": "The contract's logic assumes `getAmountOut()`'s result without verifying it, leading to potential underflow issues.",
            "prerequisites": "The contract uses the `getAmountOut()` function for calculating the output amount after a sell operation.",
            "cwe": "CWE-264: Improper Neutralization of Special Elements used in an OS Command (or similar) can lead to unauthorized access or denial of service.",
            "swc": "SWC-10: Input Validation Failure - The contract does not validate the input parameters before proceeding with any operations."
        }
    ]
}