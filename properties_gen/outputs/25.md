{
    "report_fp": "25.md",
    "analysis_resuls": [
        {
            "vulnerability_type": "Design",
            "affected_functions": [
                "CompositeMultiOracle.peek",
                "CompositeMultiOracle.get"
            ],
            "root_cause": "The issue arises because the `peek` function assumes that the final price is in 18 decimals, but it does not enforce this precision correctly.",
            "impact": "Users may receive incorrect amounts due to the mismatch between the token's decimals and the oracle's precision.",
            "recommendation": "Ensure that the prices are scaled correctly by dividing by the oracle's precision, rather than the token's decimals.",
            "affected_contracts": [
                "code-423n4/2021-08-yield-findings#26"
            ],
            "attack_vector": "Through incorrect price scaling in the `peek` and `_get` functions.",
            "prerequisites": "The oracle uses tokens with different decimals than the source token.",
            "cwe": "CWE-264: Incorrect Precision Handling",
            "swc": "SWC-103: Inconsistent Price Scaling"
        },
        {
            "vulnerability_type": "Race Condition",
            "affected_functions": [
                "ERC20Rewards._updateRewardsPerToken"
            ],
            "root_cause": "The function `_updateRewardsPerToken` exits without updating `rewardsPerToken_.lastUpdated` if `totalSupply` is zero, leading to an error when there are active rewards periods but no tokens have been minted yet.",
            "impact": "Users may receive incorrect rewards due to the lack of updates in `rewardsPerToken_.lastUpdated`, resulting in users receiving past rewards even though they should not be able to do so.",
            "recommendation": "Ensure that `rewardsPerToken_.lastUpdated` is always updated with the current time (or `end`) when `_totalSupply == 0` to avoid incorrect reward calculations.",
            "affected_contracts": [
                "ERC20Rewards"
            ],
            "attack_vector": "Race Condition",
            "prerequisites": "The contract must have a `totalSupply` of zero and an active rewards period.",
            "cwe": "CWE-193",
            "swc": "SWC-2"
        }
    ]
}