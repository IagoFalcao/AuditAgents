{
    "report_fp": "24.md",
    "analysis_resuls": [
        {
            "vulnerability_type": "Reentrancy",
            "affected_functions": [
                "swapYieldSource"
            ],
            "root_cause": "The function `swapYieldSource` can be called by the owner or asset manager, allowing them to instantly transfer all funds from the old yield source.",
            "impact": "This vulnerability allows malicious actors with either the owner's or asset manager's permission to rug all funds in the system immediately.",
            "recommendation": "Implement additional security measures such as time locks for owners and require trusted registries for Yield Sources before allowing this function.",
            "cwe": "CWE-20: Reentrancy",
            "swc": "SWC-13: Reentrancy"
        },
        {
            "vulnerability_type": "Reentrancy",
            "affected_functions": [
                "SwappableYieldSource.redeemToken"
            ],
            "root_cause": "The `transferFrom` function of ERC20 tokens checks if the contract approved itself for the `redeemableBalance`, which can lead to reentrancy attacks.",
            "impact": "Users' deposited funds may be unrecoverable due to the failure of the transaction, resulting in loss of funds.",
            "recommendation": "Use `_depositToken.safeTransfer(msg.sender, redeemableBalance)` instead of `transferFrom(address(this), msg.sender, redeemableBalance)` for transferring tokens back to the sender.",
            "cwe": "CWE-20: Reentrancy",
            "swc": "SWC-13: ERC20 Token Implementation"
        }
    ]
}