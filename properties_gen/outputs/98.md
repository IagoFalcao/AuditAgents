{
    "report_fp": "98.md",
    "analysis_resuls": [
        {
            "vulnerability_type": "Design",
            "affected_functions": [
                "_slice"
            ],
            "root_cause": "Incorrect calculation in `_slice` function leading to incorrect string representation.",
            "impact": "Potential confusion over the actual value of options, leading to incorrect representation and potential security risks.",
            "recommendation": "Update the `_slice` function to correctly calculate the length based on `bytes(_s)[_start+i]` instead of `bytes(_s)[_start+1]`.",
            "affected_contracts": [
                "options/QTokenStringUtils.sol"
            ],
            "attack_vector": "Incorrect calculation in `_slice` function leading to incorrect string representation.",
            "prerequisites": "The use of `bytes(_s)[_start+1]` instead of `bytes(_s)[_start+i]` for calculating the length of the return string.",
            "cwe": "CWE-264: Incorrect Length Calculation",
            "swc": "SWC-05: Improper String Handling"
        },
        {
            "vulnerability_type": "Arithmetic Rounding",
            "affected_functions": [
                "_mintSpread",
                "_claimCollateral"
            ],
            "root_cause": "Implicit arithmetic rounding and flawed int to uint conversion in the `_mintSpread` function.",
            "impact": "Tokens may be stolen from Controller, potentially leading to financial loss for users.",
            "recommendation": "Implement proper arithmetic rounding and ensure accurate int-to-uint conversions to prevent these vulnerabilities.",
            "cwe": "CWE-190: Arithmetic Rounding",
            "swc": "SWC-23: Implicit Arithmetic Rounding"
        }
    ]
}