{
    "report_fp": "28.md",
    "analysis_resuls": [
        {
            "vulnerability_type": "Reentrancy",
            "affected_functions": [
                "finalize"
            ],
            "root_cause": "The `finalize()` function allows anyone to call it, and it sends tokens directly to the pair pool without considering the current price of the existing pool.",
            "impact": "An attacker can control the LP price by creating a pool and then calling `finalize()` with an unfair price, minting LP tokens with a low amount of liquidity compared to the actual value of the tokens in the pool. This allows the attacker to steal most of the tokens from the pool.",
            "recommendation": "Implement proper checks on the current price of the existing pool before minting LP tokens and ensure that the amounts sent match the current market conditions.",
            "affected_contracts": [
                "PostAuctionLauncher.sol",
                "UniswapV2Pair.sol"
            ],
            "attack_vector": "The attacker can create a pool and then call `finalize()` with an unfair price, minting LP tokens with a low amount of liquidity compared to the actual value of the tokens in the pool.",
            "prerequisites": "The attack requires that the attacker has control over the token contract or is able to manipulate the state of the system.",
            "cwe": "CWE-20: Reentrancy",
            "swc": "SWC-16: Liquidity Mining Vulnerability"
        },
        {
            "vulnerability_type": "Logic Error",
            "affected_functions": [
                "SushiToken._beforeTokenTransfer"
            ],
            "root_cause": "The `_beforeTokenTransfer` function incorrectly shifts the voting power based on the `from` account instead of the `_delegates[from]` account.",
            "impact": "Transfers can revert due to incorrect shifting of votes, leading to potential loss of funds or control over tokens.",
            "recommendation": "Correctly shift the voting power by using `_delegates[from]` instead of `from` in the `_beforeTokenTransfer` function.",
            "cwe": "CWE-259: Incorrect Function Call",
            "swc": "SWC-103: Logic Error"
        }
    ]
}