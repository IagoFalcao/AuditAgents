{
    "report_fp": "10.md",
    "analysis_resuls": [
        {
            "vulnerability_type": "Logic Error",
            "affected_functions": [
                "transferERC721",
                "timeUnlockERC721"
            ],
            "root_cause": "The deletion of the timelock information is missing after a transfer, leading to incorrect matching of the recipient address.",
            "impact": "The owner who transfers an NFT back into the vault without a timelock will not match the older/stale recipient address and may fail the check on L510 unless they control that stale recipient address from the earlier timelock. This results in the token being locked forever in the vault.",
            "recommendation": "Add `delete timelockERC721s[timelockERC721Keys[nftContract][i]];` after L510 to fix the issue.",
            "cwe": "CWE-464",
            "swc": "SWC-3"
        },
        {
            "vulnerability_type": "Improper Disposal of Resources",
            "affected_functions": [
                "approveTransferERC721",
                "transferERC721"
            ],
            "root_cause": "The approval is not removed after a transfer in `transferERC721()` and there is no mechanism to revoke the approval that was granted earlier.",
            "impact": "Impact 1: The approval is not removed (set to false) after a transfer, allowing the previous/compromised delegate to transfer the NFT again. Impact 2: If a delegate becomes compromised/untrustworthy before transferring but before revoking the approval, the owner will lose its NFT.",
            "recommendation": "Add a boolean parameter `removeApproval` to `approveTransferERC721()` and set `nftApprovals` to that parameter which can be true for giving approval and false for removing/revoking approval. If `msg.sender != _getOwner()`, call `approveTransferERC721()` with the boolean false to remove approval before making a transfer in `transferERC721()` on L515.",
            "cwe": "CWE-400",
            "swc": "SWC-3"
        }
    ]
}