{
    "report_fp": "101.md",
    "analysis_resuls": [
        {
            "vulnerability_type": "Improper Calculation",
            "affected_functions": [
                "_principalWithdrawable"
            ],
            "root_cause": "The `_start()` function reduces the borrow limit by the start fee, leading to an incorrect calculation of `totalSupply[id]`.",
            "impact": "Users may be able to withdraw more principal than expected if they invoke `_start()` with a non-zero start fee.",
            "recommendation": "Implement proper calculations for `_principalWithdrawable` that account for the reduced borrow limit due to fees.",
            "affected_contracts": [
                "LenderPool.sol"
            ],
            "attack_vector": "Invoke `_start()` with a non-zero start fee.",
            "prerequisites": "The contract's implementation does not properly account for the reduced borrow limit due to fees when calculating `totalSupply[id]`.",
            "cwe": "CWE-264: Improper Limit Validation",
            "swc": "SWC-103: Incorrect Calculation"
        }
    ]
}