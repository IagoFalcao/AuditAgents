{
    "report_fp": "54.md",
    "analysis_resuls": [
        {
            "vulnerability_type": "Denial of Service (DoS)",
            "affected_functions": [
                "calculateTokensToDistribute"
            ],
            "root_cause": "The vulnerability arises from the use of `tx.gasprice` in calculating gas reimbursement, which can be manipulated by malicious actors to inflate the `tokensToDistribute` value.",
            "impact": "This could lead to arbitrary inflation of the UDT total supply, potentially resulting in a significant loss for users and miners alike. The maximum tokens that can be minted is capped at maxTokens, but it can reach up to half the total supply of UDT, which could have substantial effects on the value of UDT.",
            "recommendation": "Implement gas price protection mechanisms to prevent malicious actors from manipulating `tx.gasprice` and inflating the `tokensToDistribute` value. This would mitigate the risk of arbitrary inflation and ensure fair distribution of tokens.",
            "cwe": "CWE-4012",
            "swc": "SWC-3"
        },
        {
            "vulnerability_type": "Design/Implementation",
            "affected_functions": [
                "freeTrial",
                "shareKey"
            ],
            "root_cause": "The `freeTrial` function allows users to get full refund before the `freeTrial` ends, and a user can transfer partial of their time using `shareKey`, which makes it possible for an attacker to steal from the protocol by transferring `freeTrial` time from multiple addresses to one address.",
            "impact": "The attack vector is through transferring `freeTrial` time from multiple addresses to one address. The impact could be stealing funds from the protocol, leading to financial loss and potential reputational damage.",
            "recommendation": "Implement a proper design that prevents users from getting full refund before the `freeTrial` ends, ensuring that only the user can transfer their own `freeTrial` time using `shareKey`. This will prevent attackers from stealing funds by transferring `freeTrial` time from multiple addresses to one address.",
            "cwe": "CWE-647: Improper Access Control",
            "swc": "SWC-102: Lack of Security Considerations in Design and Implementation"
        }
    ]
}