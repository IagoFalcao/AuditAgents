{
    "report_fp": "80.md",
    "analysis_resuls": [
        {
            "vulnerability_type": "Reentrancy",
            "affected_functions": [
                "ConvexYieldWrapper.sol:Ladle.batch"
            ],
            "root_cause": "The removal of the `_beforeTokenTransfer()` function allows users to transfer `wCVX` tokens to another account without updating their balance on the original account. This can be repeated to effectively replicate a user's deposited balance across multiple accounts, leading to the loss of earned rewards.",
            "impact": "Users can duplicate their protocol-earned yield by transferring `wCVX` tokens to another account and updating their deposited balance on that new account. This effectively replicates a user's deposited balance across multiple accounts, leading to the loss of earned rewards.",
            "recommendation": "Implement `_beforeTokenTransfer()` function in the custom implementation or use an alternative method to ensure token transfers are properly handled.",
            "affected_contracts": [
                "ConvexStakingWrapper.sol",
                "Ladle.sol"
            ],
            "attack_vector": "Manual call to `user_checkpoint()` on the new account.",
            "prerequisites": "The user must have a balance of `wCVX` tokens and interact with the `batch()` function in `Ladle.sol`. The `_beforeTokenTransfer()` function has been removed from the custom implementation, allowing users to transfer `wCVX` tokens to another account after an initial checkpoint.",
            "cwe": "CWE-259",
            "swc": "SWC-01"
        },
        {
            "vulnerability_type": "Reentrancy",
            "affected_functions": [
                "_getDepositedBalance"
            ],
            "root_cause": "Vault owner can manipulate the result of `_getDepositedBalance` by checkpointing one user's balance at a given time, transferring ownership to another user and then creating a new checkpoint with this user.",
            "impact": "Users can generate protocol yield multiple times over on a single collateral amount, effectively extracting all protocol yield.",
            "recommendation": "Implement proper checks and balances in the `_getDepositedBalance` function to prevent such manipulation. Ensure that only the owner of the vault can perform actions that affect other users' balances.",
            "cwe": "CWE-20: Reentrancy",
            "swc": "SWC-13: Vault Owner Abuse"
        }
    ]
}